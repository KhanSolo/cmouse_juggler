{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/build/main.exe",
            "args": [], 
            "cwd": "${workspaceFolder}", // working directory
            "stopOnEntry": false,
            "preLaunchTask": "Build with clang (mingw64) (Debug)"
        }
    ]
}